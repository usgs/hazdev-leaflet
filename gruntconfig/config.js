'use strict';

var config = {
  build: '.build',
  devPort: 8020,
  dist: 'dist',
  distPort: 8022,
  example: 'example',
  liveReloadPort: 8029,
  src: 'src',
  test: 'test',
  testPort: 8021,
  templatePort: 8023
};

module.exports = config;
